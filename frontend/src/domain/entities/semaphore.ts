export type Semaphore = {
  administration: string
  base: string
  department: string
  email: string
  facade: string
  geom?: { coordinates: number[]; type: string }
  id: number
  name: string
  phoneNumber: string
  unit: string
}
