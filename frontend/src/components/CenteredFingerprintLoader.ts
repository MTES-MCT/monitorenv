import { FingerprintLoader } from '@mtes-mct/monitor-ui'
import styled from 'styled-components'

export const CenteredFingerprintLoader = styled(FingerprintLoader)`
  position: initial !important;
  display: block;
  margin-top: 300px;
`
